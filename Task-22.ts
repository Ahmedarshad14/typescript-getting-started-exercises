const myArr:string[] = ["Ahmed", "Ali", "Haris"]

/* intentional Error, trying to access index 3 which is non-existent
 It will throw undefined when we try to access it. */
console.log(myArr[3])

// We can console log the available indexes like this

console.log(myArr[0])
console.log(myArr[1])
console.log(myArr[2])

